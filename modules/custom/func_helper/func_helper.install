<?php

function func_helper_install()
{
  // установить добавленную данным модулем путь в качестве главной
  \Drupal::configFactory()->getEditable('system.site')
    ->set('page.front', '/home')
    ->save();
}
