<div class="cart-block">
  <a href="/cart">
    <div class="cart-block__inner">
      <i class="picon picon--bag picon--left"></i>Корзина
      <span class="cart-badge-wrapper">
        {% if count %}
        <span class="badge">{{ count }}</span>
        {% endif %}
      </span>
    </div>
  </a>
</div>
